spring:
  config:
    import: "optional:configserver:"
  main:
    allow-bean-definition-overriding: true
  mongodb:
    embedded:
      version: 4.0.2
  data:
    mongodb:
      database: piggymetrics
      port: 0